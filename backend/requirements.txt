# CONTROLPC backend dependencies.
# Versions are intentionally unpinned (minimums only) so wheels resolve on
# newer Python runtimes (e.g. 3.13 / 3.14) instead of failing on stale pins.
# GPU inference is handled by an external bundled llama.cpp `llama-server.exe`
# (CUDA), so `llama-cpp-python` is NOT required.
fastapi>=0.111
uvicorn[standard]>=0.30
pyautogui>=0.9.54
pillow>=10.3
requests>=2.32
pydantic>=2.7
python-multipart>=0.0.9
mss>=9.0
pywebview>=5.0
pywin32>=306
pywinauto>=0.6.8
python-docx>=1.1
keyring>=25.0
pytest>=8.0
